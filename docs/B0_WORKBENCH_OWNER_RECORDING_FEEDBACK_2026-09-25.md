# B0 / Workbench Owner Recording — Dense Feedback Extraction

**Date:** 2026-09-25  
**Recording:** `20260925-2150-25.3534690.mp4`  
**Duration:** 160.2 s  
**Visual:** 1866×912 @ 30 fps  
**Audio:** silent  
**Exact public candidate observed:** `43c8a3fdf1bc12110e34c53159841b363b4414ea`

## 0. Owner verdict

Explicit Owner feedback after the recording:

> "jest w dobrym kierunku"

Owner requested:

- end the current stage solidly and professionally;
- polish what exists;
- prepare for later Owner instructions;
- make stage completion visually / materially clear.

This is a positive directional signal, not a promotion of B0's equations to final Feniks semantics.

## 1. Dense observed timeline

### 00:00–00:15 — direct Workbench entry

Observed:

- B0 loads and runs immediately;
- Owner manipulates Body envelope directly;
- around 00:04 the visible envelope is ~1.45;
- Owner immediately combines parameter editing with movement/world contact.

Interpretation:

> the Inspector is discoverable enough to begin exploration without memorized shortcut instructions.

### 00:15–00:35 — live parameter navigation

Observed:

- Owner scrolls naturally through Mass & load and Locomotion;
- live edits continue while the world remains running;
- around 00:35:
  - radius ~23.58;
  - total mass 5;
  - force 2;
  - acceleration ~304;
  - max speed 225.

Interpretation:

> authored vs derived relations are sufficiently visible for exploratory tuning.

### 00:35–01:10 — deliberate extreme pressure

Observed:

- intrinsic body mass reaches the current hard rail 20;
- carried load is 4;
- locomotor force is raised;
- around 00:58 Owner manually enters **42** into Locomotor force;
- the field temporarily displays 42 while focused;
- runtime clamps to current safety rail 8 and the field later shows `8.00 EXTREME`.

Material finding:

> **the current safety rail is too tight for the Owner's actual exploratory behavior.**

Additional truthfulness finding:

> **during a clamped numeric edit, the visible focused field can temporarily disagree with the value actually applied by the simulation.**

This violates the Workbench requirement that current authored state be legible and truthful.

### 01:10–01:40 — orthogonality used spontaneously

Observed:

- around 01:16:
  - envelope ~0.70;
  - intrinsic body mass 20;
  - carried load 4;
  - force 8;
- Owner moves a small but very heavy / high-force actor through the field;
- shortly afterward envelope is pushed to the opposite current rail 4.50 while mass/load/force remain extreme;
- the giant body produces materially different route/clearance/contact behavior.

Interpretation:

> the Owner does not merely observe independent axes; he spontaneously **uses** them to create non-correlated phenotypes.

This is the strongest human evidence yet for the decomposition principle:

> body envelope and inertial / locomotor properties should not be permanently fused.

It does **not** establish the final set of Feniks body variables.

### 01:40–01:52 — Debug + reset / live-state inspection

Observed:

- Owner enables Debug;
- debug overlay is then used while continuing movement and edits;
- world reset / restart behavior is exercised;
- envelope is rapidly reduced from the giant state while other dimensions remain independently editable.

Interpretation:

- Debug is discoverable and useful enough to survive real play;
- live numerical overlay is useful when the Inspector is scrolled elsewhere;
- world reset belongs in the permanent Workbench loop.

### 01:52–02:26 — transition from breaking to tuning

Observed:

- configuration returns toward ordinary envelope/body values while force remains high;
- load is tuned around ~0.95;
- force is later reduced to ~0.85;
- around 02:20:
  - envelope ~1.62;
  - intrinsic body mass ~1.60;
  - load ~0.95;
  - force ~0.85;
- Owner continues moving/contacting rather than abandoning the experiment.

Interpretation:

> the Workbench supports both "break it" exploration and finer convergence/tuning.

This is important because the desired lab loop is not only extreme stress testing.

### A/B behavior

Observed:

- one configuration is captured into slot A;
- slot B remains unused in the recording;
- no complete Owner A↔B apply / matched-start loop is observed.

Status:

> **A/B Workbench usefulness remains UNPROVEN by Owner behavior.**

Machine/browser qualification remains valid, but do not promote human usefulness.

### 02:26–02:40 — experiment switching

Observed:

- Owner opens the experiment selector;
- switches from B0 to S0;
- then switches to the substrate smoke probe;
- each experiment runs inside the same shell.

Interpretation:

> the Workbench is being used as a multi-experiment environment rather than as a one-off B0 page.

Polish implication:

- the smoke probe is internal apparatus and should be visually distinguished from research experiments in the selector.

## 2. Qualified human signals

### Workbench direct manipulation — PASS

Observed use without chat-side shortcut memorization:

- slider / numeric editing;
- scrolling parameter groups;
- per-parameter reset affordances;
- Reset World;
- Debug;
- experiment switching;
- one parameter-state capture.

### B0 human discriminability — PASS, narrow

Owner behavior demonstrates that changes in:

- body envelope;
- inertial mass/load;
- locomotor force

are perceptible enough to motivate continued manipulation and materially different play.

This is not a claim that current magnitudes or formulas are correct.

### Embodiment decomposition direction — POSITIVE OWNER SIGNAL

The recording plus explicit "jest w dobrym kierunku" supports continuing the broader principle that:

> **spatial envelope, inertial burden and locomotor authority need not be one permanently correlated character stat.**

### Feniks semantic promotion — NO

Still unqualified:

- final body model;
- final mass units;
- final equipment/load model;
- final acceleration/braking law;
- traction;
- stance;
- rotational inertia;
- humanoid collider representation;
- combat consequences.

## 3. Material findings requiring closure polish

### F1 — safety rails too restrictive

Direct Owner evidence:

- body mass hits 20;
- envelope hits 4.50;
- locomotor force is explicitly entered as 42 and rejected down to 8.

Action:

- widen numerical safety rails substantially;
- retain soft ranges as convenient normal regions;
- validate the wider rails for finite/stable runtime;
- do not convert wide exploration into "invalid build" behavior.

### F2 — numeric truthfulness during clamp

Observed:

- entered value can remain visible while the simulation has already clamped to another value.

Action:

- after every edit, the number field must reflect the **applied authored value**, even while focused;
- if a numerical safety rail is ever reached, display a narrow factual safety-rail indicator rather than silently lying.

### F3 — internal diagnostic experiment presentation

Observed:

- Owner switches from B0 → S0 → Substrate smoke probe.

Action:

- retain the smoke probe for infrastructure diagnosis;
- classify it visually as an internal diagnostic in the experiment selector;
- do not present it as a peer research experiment.

### F4 — panel density

Observed:

- real four-axis B0 use requires repeated Inspector scrolling;
- this is acceptable, but descriptions consume meaningful vertical space.

Action:

- shorten B0 descriptions without removing causal meaning;
- do not add new sections/features during closure.

## 4. Explicit non-findings

Do not infer from this recording that:

- A/B comparison is human-qualified;
- Owner prefers a particular mass/force ratio;
- current hard extremes are desired final gameplay values;
- Debug should become normal player UI;
- circular bodies are accepted as final representation;
- B0 is a combat model.

## 5. Closure target

The current stage may close when all of the following are true:

1. F1–F4 are addressed without opening a new research question;
2. widened rails remain finite under deterministic stress;
3. numeric fields always display applied truth;
4. Workbench source + emitted artifact browser gates PASS;
5. normal / extreme / compact screenshots are manually inspected;
6. exact public origin is verified after final deploy;
7. canonical state clearly marks:
   - Workbench foundation: qualified for current research use;
   - B0 decomposition direction: positive Owner signal;
   - final Feniks semantics: unproven;
   - A/B human usefulness: unproven;
8. the branch is left clean and ready for new Owner instructions.

## Stage-closing invariant

> **Preserve the discovered experimental freedom; do not turn the closure pass into the next experiment.**
