# R3 Owner Contact — Live State

**Date:** 2026-09-25  
**Campaign:** active; machine gate complete  
**Exact deployed observation specimen:** `b51aad2dbc6475237b9fb22fdb0ed24b82bb6827`  
**Owner play:** not yet performed

## Live truth

R3 remains **JOINT-AFTERSTATE CAUSAL-KERNEL QUALIFIED**.

The Owner Contact Specimen is not R4 and is not a combat organism. It exists only to expose that kernel to direct human manipulation before adversarial meaning is added.

Hard boundary:

> **R3 kernel qualified ≠ R3 human phenomenon qualified.**

## Machine gate

| Gate | Status |
| --- | --- |
| Exact qualified R3 kernel restored | PASS |
| R3 + specimen CI | **15 / 15 PASS** |
| Starts outside material contact | PASS |
| Deliberate ordinary entry can create contact | PASS |
| Ordinary locomotion can leave contact | PASS |
| Exact Pages source / artifact provenance | PASS |
| ES-module runtime in Chromium | PASS |
| Missing module / page / console errors | NONE |
| Human feel / readability | **UNPROVEN** |

The public Pages target is:

`https://jozzpoly.github.io/Combat-Lab/`

The Pages build for this gate checked out the exact deployed SHA above.

## Browser boundary

The Opera Browser Connector was retried repeatedly and remained disconnected.

That does **not** count as a specimen failure and does not qualify the public URL from the agent side.

To avoid pretending runtime evidence existed, the exact emitted Pages artifact was downloaded and exercised in Chromium with its versioned ES-module import graph preserved.

## Earlier superseded lineage

Workflow history recovered a previous Owner-contact attempt:

- `fe76e7a16e2ce31a98ab0c84e2119fac5e5d22bf`;
- deployed `2b56e5c5e9e14fa8269d260d45e827b36640d6d5`;
- boundary record `a64f250e6e82d30dfad47f12e45d08451fd1c224`.

It contains no Owner play evidence. Its real-browser gate was explicitly unproven and it was later superseded by the reset lineage.

Preserve it as apparatus/provenance history.

## Next valid action

No further pre-Owner feel tuning.

Owner instruction should stay minimal:

> WASD — move. Mouse — guide the tool. Left click — COMMIT. R — reset. Play with contact and report what you notice.

After raw Owner evidence:

- if imperceptible, separate translation failure from kernel failure;
- if perceptible but sticky/chore-like/arbitrary, retain the donor and stop human-facing promotion;
- if perceptible and interesting, only then open the R4 adversarial-pressure hypothesis;
- if an unexpected phenomenon dominates, follow that evidence instead of the roadmap.
