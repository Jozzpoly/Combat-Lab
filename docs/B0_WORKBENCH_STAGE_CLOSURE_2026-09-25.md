# Combat Lab vNext — B0 / Workbench Stage Closure

**Date:** 2026-09-25  
**Status:** **STAGE CLOSED — research foundation preserved, next direction intentionally open**  
**Owner recording:** 160.2 s direct play on public B0 Workbench  
**Owner verdict:** "jest w dobrym kierunku"  
**Authority note:** this file preserves the stage-closure truth of 2026-09-25. The later `WAITING FOR OWNER INSTRUCTION` wording describes that handoff moment; new Owner instruction arrived on 2026-09-26 and live sequencing now belongs to `RESEARCH_STATE.md`.

## 1. What this stage actually established

### Workbench

**OWNER-OBSERVED PASS**

The Workbench is now a usable research instrument rather than a shortcut-driven demo shell.

Direct Owner behavior observed:

- parameter sliders and numeric entry;
- live editing while simulation runs;
- per-parameter resets;
- Reset World;
- Debug;
- experiment switching;
- one parameter-state capture.

The Owner did not require chat-side memorization to operate the experiment.

### Embodiment decomposition

**POSITIVE OWNER SIGNAL**

The Owner spontaneously explored non-correlated configurations, including:

- small envelope + very high mass/load/force;
- giant envelope + the same heavy/high-force configuration;
- later moderate / finely tuned combinations.

Therefore the stage supports the principle:

> **spatial envelope, inertial burden and locomotor authority should remain separable research dimensions unless future evidence justifies correlating them.**

### B0 human discriminability

**PASS, narrow**

The current B0 axes create differences visible/usable enough to sustain direct exploration.

This does not qualify the current equations or magnitudes as final Feniks semantics.

## 2. What remains deliberately unproven

- final Feniks actor/body model;
- final body / equipment mass units;
- final locomotor-force law;
- final acceleration / braking law;
- top-speed coupling;
- traction / footing;
- stance;
- rotational inertia;
- humanoid collision geometry;
- armour geometry / joint restriction;
- combat consequences;
- A/B human usefulness.

A/B remains **UNPROVEN by Owner behavior** because the recording contains one A capture but no completed A↔B comparison loop.

## 3. Recording-driven closure fixes

### Permissive exploration

The old B0 safety rails were materially too narrow.

Owner evidence:

- envelope reached 4.50;
- intrinsic body mass reached 20;
- force 42 was manually entered while old force rail was 8.

Final B0 rails:

- envelope: **0.05 .. 12**;
- intrinsic body mass: **0.01 .. 200**;
- carried load mass: **0 .. 200**;
- locomotor force: **0.01 .. 100**.

Soft ranges remain narrow convenience zones.

Crossing them is allowed and marked `EXTREME`, not rejected.

### Truthful numeric editing

The old Workbench could briefly display a requested numeric value while runtime had already clamped it.

Closure behavior:

- every committed edit immediately displays the **applied authored value**;
- true numerical safety boundaries display `SAFETY RAIL`;
- Owner-observed force 42 is legal and remains 42.

### Panel density

B0 descriptions were shortened while preserving causal meaning.

No new research controls were added.

### Diagnostic separation

`Substrate smoke probe` remains available for infrastructure work but is grouped under:

> **Internal diagnostics**

B0 and S0 remain under:

> **Research experiments**

## 4. Final internal evidence before closure deploy

Code checkpoint:

`9a58da5f99202fb777f3a5a066dedeea86370cfb`

Qualification:

- **31 / 31 automated checks PASS**;
- real Chromium Workbench gate PASS;
- force 42 regression PASS:
  - runtime applies 42;
  - numeric field displays 42;
  - state is EXTREME, not safety-clamped;
- true beyond-rail input is immediately reconciled to applied value and visibly marked `SAFETY RAIL`;
- widened rail stress remains finite;
- experiment classification / selector grouping PASS;
- existing A/B matched-motion gate PASS;
- focus isolation PASS;
- world/default reset separation PASS;
- B0 ↔ S0 switching PASS;
- compact horizontal-overflow audit PASS.

Manual screenshot inspection:

- normal 1600×1000: PASS;
- Owner-derived extreme state: PASS;
- compact 1280×800: PASS.

## 5. Closure boundary

This stage is not being closed because Combat Lab is solved.

It is being closed because the current question has produced enough value and the research instrument is now strong enough to preserve that value without forcing the next question.

No next combat family is selected here.

The next Owner instruction may:

- continue embodiment;
- pivot to terrain / stance / equipment;
- return to tools / shields / reach;
- open bows / projectiles;
- open magic;
- combine modalities;
- or challenge the foundation itself.

The Workbench should support that decision rather than pre-empt it.

## Canonical stage verdict

> **WORKBENCH FOUNDATION — QUALIFIED FOR CURRENT RESEARCH USE**

> **B0 EMBODIMENT DECOMPOSITION — POSITIVE OWNER SIGNAL / STAGE CLOSED**

> **FINAL FENIKS BODY OR COMBAT MODEL — NOT DECIDED**

> **NEXT RESEARCH DIRECTION — OPEN, WAITING FOR OWNER INSTRUCTION**


## 6. Exact final public closure

Final deployed closure candidate:

`2eb9a878eb4fa9c406ca0e90abcab33cb186332a`

CI / Pages:

- check run `36195359528`: SUCCESS;
- emitted artifact browser gate: PASS;
- Pages run `36195381040`: SUCCESS;
- Pages build checked out exact `2eb9a878eb4fa9c406ca0e90abcab33cb186332a`;
- Pages artifact ID: `10889951339`;
- deploy: SUCCESS.

Real public-browser verification:

- existing Combat Lab tab in Opera was explicitly navigated/reloaded to the public origin;
- active experiment: `Load / Envelope Field B0`;
- runtime surface present;
- final shortened B0 Inspector copy present;
- selector exposes:
  - `Research experiments` → S0, B0;
  - `Internal diagnostics` → Substrate smoke probe;
- direct public `COMMIT.txt` opened in Opera returns exact:
  - `2eb9a878eb4fa9c406ca0e90abcab33cb186332a`.

Therefore:

> **repo truth = emitted artifact truth = real public-browser truth.**

No additional Owner test is required to close this stage because the final changes are bounded recording-driven polish, not a new research mechanism.

## Final handoff state

The project is intentionally left with:

- a qualified Workbench substrate;
- a positive B0 embodiment-decomposition signal;
- preserved S0 reference experiment;
- internal substrate diagnostic clearly separated;
- no preselected next combat family;
- no open implementation obligation from this stage.

**READY FOR NEW OWNER INSTRUCTION.**
