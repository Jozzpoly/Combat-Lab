name: legacy-pages-r0-disabled

on:
  workflow_dispatch:

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy workflow disabled."
          echo "Use .github/workflows/pages.yml."
